import { createContext } from "react"

const SiteContext = createContext({} as any)
const HomeContext = createContext({} as any)
const ProductContext = createContext({} as any)
const SingleProuctContext = createContext({} as any)
const CheckoutContext = createContext({} as any)

export {
	SiteContext,
	HomeContext,
	ProductContext,
	SingleProuctContext,
	CheckoutContext,
}
